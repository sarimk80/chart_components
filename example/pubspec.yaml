name: example
description: Example project for chart_components package.

version: 1.0.0+1

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5

  chart_components:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true

